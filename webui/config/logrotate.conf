# nginx-webui log rotation.
# Deployed to: /opt/etc/logrotate.d/nginx-webui
# Requires: logrotate package + cron (cron.daily runs logrotate).

/opt/var/log/nginx-webui-access.log
/opt/var/log/nginx-webui-error.log {
    daily
    rotate 3
    missingok
    notifempty
    compress
    delaycompress
    postrotate
        [ -f /opt/var/run/nginx-webui.pid ] && kill -USR1 "$(cat /opt/var/run/nginx-webui.pid)" 2>/dev/null || true
    endscript
}
