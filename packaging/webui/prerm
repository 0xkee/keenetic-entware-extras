#!/opt/bin/sh
# prerm — runs before package files are removed.
set -eu

# 1. Stop service (|| true: symlink may be absent if user disabled)
/opt/etc/init.d/S80nginx-webui stop 2>/dev/null || true

# 2. Remove init.d symlink
rm -f /opt/etc/init.d/S80nginx-webui || true
