# Anomaly page markers — one string per line.
# If HTTP response body contains any of these markers,
# the check result is flagged as "Content anomaly".
# Used to detect ISP/DPI block pages that return HTTP 200.
<title>Blocked
<title>Access Denied
<title>Website Blocked
nfgw.
blocked.html
warn.rt.ru
blackhole
rkn.gov.ru
requestblocked
eais.by
zapret
